
@inject IUnitOfWork<SiteInfo> _siteInfo
@inject UserManager<ApplicationUser> _userManager
@inject Microsoft.Extensions.Localization.IStringLocalizer<NewsApp2.SharedViewResource> L
@using System.Globalization

<!DOCTYPE html>
<html lang="@CultureInfo.CurrentUICulture.Name" dir="@(CultureInfo.CurrentUICulture.TextInfo.IsRightToLeft ? "rtl" : "ltr")">
<head>
    <meta name="viewport" content="width=device-width" />
    @* --------------------------------------------------------------- *@
    @{
        var siteInfo = _siteInfo.Repository.GetAll().SingleOrDefault();
        ViewBag.SiteName = @siteInfo?.Name;
        ViewBag.LogoUrl = @siteInfo?.LogoUrl;
        var isRtl = CultureInfo.CurrentUICulture.TextInfo.IsRightToLeft;
    }

    <partial name="Partial/_title"></partial>
    @* --------------------------------------------------------------- *@
    @if (isRtl)
    {
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    }
    else
    {
        <environment include="Development">
            <link href="~/lib/bootstrap/css/bootstrap.min.css" rel="stylesheet" />
        </environment>
        <environment include="Staging,Production">
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        </environment>
    }


    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href="https://cdn.jsdelivr.net/npm/tom-select@2.3.1/dist/css/tom-select.bootstrap5.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i" rel="stylesheet" />
    @*------------------------------ Dashboard template -------------------------------*@
    <link rel="stylesheet" href="~/assets1/vendors/mdi/css/materialdesignicons.min.css">
    <link rel="stylesheet" href="~/assets1/css/style.css">
    @* ------------------------------------------------------------------------------ *@
    <link href="~/css/template.css" rel="stylesheet" />
    <link href="~/css/dashboard.css" rel="stylesheet" />
    <link href="~/css/date-inputs.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css" rel="stylesheet" />
    @if (isRtl)
    {
        <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet" />
        <link href="~/css/rtl-support.css" rel="stylesheet" />
        <style>
            body { font-family: 'Cairo', sans-serif; }
        </style>
    }
    @* ------------------------------------------------------------------------------ *@
</head>
<body>

    <div class="container-scroller">
        <!-- partial:partials/_navbar.html -->
        <nav class="navbar default-layout-navbar col-lg-12 col-12 p-0 fixed-top d-flex flex-row border-bottom">
            <div class="text-center navbar-brand-wrapper d-flex align-items-center justify-content-center">

                @if (ViewBag.LogoUrl != null)
                {
                    <a class="navbar-brand brand-logo" asp-controller="StockBalances" asp-action="Index"><img src="~/upload/@ViewBag.LogoUrl" alt="logo" style="width:40px;height:40px;" /></a>
                    <a class="navbar-brand brand-logo-mini" asp-controller="StockBalances" asp-action="Index"><img src="~/upload/@ViewBag.LogoUrl" alt="logo" style="width:40px;height:40px;" /></a>
                }
                else
                {

                    <a class="navbar-brand brand-logo" asp-controller="StockBalances" asp-action="Index"><img src="~/img/logo.png" alt="logo" style="width:40px;height:40px;" /></a>
                    <a class="navbar-brand brand-logo-mini" asp-controller="StockBalances" asp-action="Index"><img src="~/img/logo.png" alt="logo" style="width:40px;height:40px;" /></a>
                }
            </div>
            <div class="navbar-menu-wrapper d-flex align-items-stretch">
                <a class="navbar-toggler navbar-toggler align-self-center mt-4 fs-2" style="cursor:pointer" data-toggle="minimize">
                    <span class="mdi mdi-menu"></span>
                </a>

                <div class="d-none d-md-flex align-items-center ms-3 me-2">
                    <partial name="Partial/_LanguageSwitcher"></partial>
                </div>

                <ul class="navbar-nav navbar-nav-right">
                    <li class="nav-item nav-profile dropdown">
                        <a class="nav-link dropdown-toggle" id="profileDropdown" href="#" data-toggle="dropdown" aria-expanded="false">

                            <!--------------------------------------------------------------------------------->
                            <div class="nav-profile-img">
                                <img src="~/img/User.png" alt="no img" />
                            </div>
                            <div class="nav-profile-text">
                                <p class="mb-1 text-black">@User.Identity.Name</p>
                            </div>

                            <!--------------------------------------------------------------------------------->
                        </a>
                        <div class="dropdown-menu navbar-dropdown dropdown-menu-right p-0 border-0 font-size-sm" aria-labelledby="profileDropdown" data-x-placement="bottom-end">
                            <div class="p-3 text-center bg-primary">
                                <img class="img-avatar img-avatar48 img-avatar-thumb" src="~/img/User.png" alt="">
                            </div>
                            <div class="p-2">
                                <h5 class="dropdown-header text-uppercase  pl-2 text-dark mt-2">@L["Account"]</h5>
                                <a class="dropdown-item py-1 d-flex align-items-center justify-content-between" asp-action="ChangePassword" asp-controller="Account">
                                    <span>@L["ChangePassword"]</span>
                                    <i class="mdi mdi-lock-reset"></i>
                                </a>
                                <div role="separator" class="dropdown-divider"></div>
                                <h5 class="dropdown-header text-uppercase  pl-2 text-dark mt-2">@L["SignedIn"]</h5>

                                <a class="dropdown-item py-1 d-flex align-items-center justify-content-between" asp-action="Logout" asp-controller="Account">
                                    <span>@L["LogOut"]</span>
                                    <i class="mdi mdi-logout ml-1"></i>
                                </a>
                            </div>
                        </div>
                    </li>
                </ul>
            </div>
            @* يظهر في الشاشات الصغيرة *@
            <button class="navbar-toggler navbar-toggler-right d-lg-none align-self-center me-2 fs-3" type="button" data-toggle="offcanvas">
                <span class="mdi mdi-menu"></span>
            </button>
        </nav>

        <!-- partial -->
        <div class="container-fluid page-body-wrapper">
            <nav class="sidebar sidebar-offcanvas" id="sidebar">
                <ul class="nav">
                    <li class="nav-item">
                        <a href="@Url.Action("Index", "StockBalances")" class="nav-link ajax-link">
                            <span class="icon-bg"><i class="bi bi-box-seam menu-icon"></i></span>
                            <span class="menu-title">@L["InventoryDashboard"]</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="@Url.Action("SendEmail", "Mail")" class="nav-link ajax-link">
                            <span class="icon-bg"><i class="bi bi-envelope-arrow-up menu-icon"></i></span>
                            <span class="menu-title">@L["EmailSend"]</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <span class="nav-link disabled text-uppercase small fw-semibold">@L["Inventory"]</span>
                    </li>
                    <li class="nav-item">
                        <a href="@Url.Action("Index", "Categories")" class="nav-link ajax-link">
                            <span class="icon-bg"><i class="bi bi-tags menu-icon"></i></span>
                            <span class="menu-title">@L["Categories"]</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="@Url.Action("Index", "Items")" class="nav-link ajax-link">
                            <span class="icon-bg"><i class="bi bi-boxes menu-icon"></i></span>
                            <span class="menu-title">@L["Items"]</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="@Url.Action("Index", "StockBalances")" class="nav-link ajax-link">
                            <span class="icon-bg"><i class="bi bi-clipboard-data menu-icon"></i></span>
                            <span class="menu-title">@L["StockOverview"]</span>
                        </a>
                    </li>
                    @if (!User.IsInRole("Cashier"))
                    {
                        <li class="nav-item">
                            <a href="@Url.Action("Movement", "StockBalances")" class="nav-link ajax-link">
                                <span class="icon-bg"><i class="bi bi-graph-up menu-icon"></i></span>
                                <span class="menu-title">@L["StockMovement"]</span>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="@Url.Action("DailyInventory", "StockBalances")" class="nav-link ajax-link">
                                <span class="icon-bg"><i class="bi bi-calendar-check menu-icon"></i></span>
                                <span class="menu-title">تقرير الجرد اليومي</span>
                            </a>
                        </li>
                    }
                    @if (User.IsInRole("Prog") || User.IsInRole("Admin") || User.IsInRole("SalesManager"))
                    {
                        <li class="nav-item">
                            <a href="@Url.Action("ProfitLoss", "FinancialReports")" class="nav-link ajax-link">
                                <span class="icon-bg"><i class="bi bi-cash-coin menu-icon"></i></span>
                                <span class="menu-title">@L["FinancialReport"]</span>
                            </a>
                        </li>
                    }
                    @if (!User.IsInRole("Cashier"))
                    {
                        <li class="nav-item">
                            <a href="@Url.Action("Index", "Expenses")" class="nav-link ajax-link">
                                <span class="icon-bg"><i class="bi bi-wallet2 menu-icon"></i></span>
                                <span class="menu-title">المصروفات والمرتبات</span>
                            </a>
                        </li>
                    }
                    @if (User.IsInRole("Admin") || User.IsInRole("Prog") || User.IsInRole("SalesManager"))
                    {
                        <li class="nav-item">
                            <a href="@Url.Action("Index", "PurchaseInvoices")" class="nav-link ajax-link">
                                <span class="icon-bg"><i class="bi bi-cart-plus menu-icon"></i></span>
                                <span class="menu-title">@L["Purchases"]</span>
                            </a>
                        </li>
                    }
                    <li class="nav-item">
                        <a href="@Url.Action("Create", "SalesInvoices")" class="nav-link ajax-link">
                            <span class="icon-bg"><i class="bi bi-receipt-cutoff menu-icon"></i></span>
                            <span class="menu-title">نقطة البيع</span>
                        </a>
                    </li>
                    @if (!User.IsInRole("Cashier"))
                    {
                        <li class="nav-item">
                        <a href="@Url.Action("Index", "SalesInvoices")" class="nav-link ajax-link">
                            <span class="icon-bg"><i class="bi bi-cart-check menu-icon"></i></span>
                            <span class="menu-title">@L["Sales"]</span>
                        </a>
                    </li>
                    }
                    @if (User.IsInRole("Admin") || User.IsInRole("Prog") || User.IsInRole("SalesManager"))
                    {
                        <li class="nav-item">
                            <a href="@Url.Action("CashierPerformance", "PosShifts")" class="nav-link ajax-link">
                                <span class="icon-bg"><i class="mdi mdi-chart-line menu-icon"></i></span>
                                <span class="menu-title">تقرير أداء الكاشير</span>
                            </a>
                        </li>
                    }
                    <li class="nav-item">
                        <a href="@Url.Action("Index", "Customers")" class="nav-link ajax-link">
                            <span class="icon-bg"><i class="mdi mdi-account-group menu-icon"></i></span>
                            <span class="menu-title">@L["Customers"]</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="@Url.Action("Debts", "Customers")" class="nav-link ajax-link">
                            <span class="icon-bg"><i class="mdi mdi-cash-multiple menu-icon"></i></span>
                            <span class="menu-title">ديون العملاء</span>
                        </a>
                    </li>
                    @if (User.IsInRole("Prog") || User.IsInRole("Admin"))
                    {
                        <li class="nav-item">
                            <a class="nav-link" data-toggle="collapse" href="#users" aria-expanded="false" aria-controls="auth">
                                <span class="icon-bg"><i class="bi bi-people menu-icon"></i></span>
                                <span class="menu-title">@L["Users"]</span>
                                <i class="menu-arrow"></i>
                            </a>
                            <div class="collapse" id="users">
                                <ul class="nav flex-column sub-menu">
                                    <li class="nav-item">
                                        <a href="@Url.Action("Index", "Employees")" class="nav-link ajax-link">
                                            <i class="bi bi-person"></i>
                                            <span class="menu-title">@L["Employees"]</span>
                                        </a>
                                    </li>
                                    @if (User.IsInRole("Prog"))
                                    {
                                        <li class="nav-item">
                                            <a href="@Url.Action("Index", "Roles")" class="nav-link ajax-link">
                                                <i class="bi bi-shield-lock"></i>
                                                <span class="menu-title">@L["Roles"]</span>
                                            </a>
                                        </li>
                                    }
                                </ul>
                            </div>
                        </li>
                    }
                    <li class="nav-item ">
                        <a class="nav-link" data-toggle="collapse" href="#settings" aria-expanded="false" aria-controls="auth">
                            <span class="icon-bg"><i class="bi bi-gear menu-icon"></i></span>
                            <span class="menu-title">@L["Settings"]</span>
                            <i class="menu-arrow"></i>
                        </a>
                        <div class="collapse" id="settings">
                            <ul class="nav flex-column sub-menu">
                                <li class="nav-item">

                                    @if(User.IsInRole("Prog"))
                                    {
                                        <a href="@Url.Action("SiteState", "Admin")" class="nav-link ajax-link">
                                            @L["SiteState"]
                                        </a>
                                    }  
                                    
                                </li>
                                <li class="nav-item">
                                    <a href="@Url.Action("SiteDetails", "Admin")" class="nav-link ajax-link">
                                        @L["SiteInfoContact"]
                                    </a>
                                </li>
                                <li class="nav-item">
                                    <a href="@Url.Action("EmailSettings", "Mail")" class="nav-link ajax-link">
                                        @L["EmailSettings"]
                                    </a>
                                </li>
                                @if (User.IsInRole("Admin") || User.IsInRole("Prog") || User.IsInRole("SalesManager"))
                                {
                                    <li class="nav-item">
                                        <a href="@Url.Action("Index", "AuditLogs")" class="nav-link ajax-link">
                                            @L["AuditLogs"]
                                        </a>
                                    </li>
                                }
                                @* <li class="nav-item">
                                    <a href="@Url.Action("GoToBlazor", "Admin")" class="nav-link ajax-link">
                                        Blazor Page
                                    </a>
                                </li> *@
                            </ul>
                        </div>
                    </li>
                    <li class="nav-item sidebar-user-actions">
                        <div class="sidebar-user-menu">
                            <a href="@Url.Action("Logout", "Account")" class="nav-link ajax-link">
                                <i class="mdi mdi-logout menu-icon"></i>
                                <span class="menu-title">@L["LogOut"]</span>
                            </a>
                        </div>
                    </li>
                </ul>
            </nav>

            <!-- partial -->
            <div class="main-panel">
                <div class="content-wrapper">
                    @RenderBody()
                </div>
                <footer class="footer ">
                    <div class="footer-inner-wraper">
                        <div class="d-sm-flex justify-content-center justify-content-sm-between">
                            @L["Copyright"] &copy; @DateTime.Now.Year - @ViewBag.SiteName
                        </div>
                    </div>
                </footer>
            </div>
        </div>
    </div>

    @*------------------------------ Dashboard template -------------------------------*@
    <script src="~/assets1/vendors/js/vendor.bundle.base.js"></script>
    <script src="~/assets1/js/off-canvas.js"></script>
    <script src="~/assets1/js/misc.js"></script>
    <script src="~/assets1/js/hoverable-collapse.js"></script>
    @* ------------------------------------------------------------------------------ *@

    <environment include="Development">
        <script src="~/lib/jquery/jquery.min.js"></script>
        <script src="~/lib/bootstrap/js/bootstrap.bundle.min.js"></script>
    </environment>
    <environment include="Staging,Production">
        <script src="https://code.jquery.com/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
    </environment>

    <script src="https://cdn.jsdelivr.net/npm/tom-select@2.3.1/dist/js/tom-select.complete.min.js"></script>
    <script src="~/js/lines-table.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
    <script src="https://cdn.jsdelivr.net/npm/flatpickr/dist/l10n/ar.js"></script>
    <script src="~/js/date-picker.js"></script>
    <script>
        (function () {
            function initSelect(el) {
                if (!el || el.tomselect || el.dataset.noTs !== undefined) return;
                new TomSelect(el, {
                    create: false,
                    allowEmptyOption: true,
                    sortField: { field: "text", direction: "asc" },
                    dropdownParent: 'body',
                    placeholder: el.dataset.placeholder || '',
                    hidePlaceholder: true
                });
            }

            document.querySelectorAll('select.form-select').forEach(initSelect);
            document.addEventListener('ts:refresh', () => {
                document.querySelectorAll('select.form-select').forEach(initSelect);
            });
        })();
    </script>
    
    @* --------------------------------- *@
    <partial name="Partial/_toast"></partial>
    @* --------------------------------- *@
 
    @await RenderSectionAsync("Scripts", required: false)

</body>
</html>

